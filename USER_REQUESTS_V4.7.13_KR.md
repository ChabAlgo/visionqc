# VisionQC v4.7.13 사용자 요청 및 반영 기준

## 이번 버전 요청

1. 이미지 분류 화면에서 100%를 초과해 확대해도 다음·이전 이미지로 이동할 때 보고 있던 좌표와 배율을 유지한다.
2. 키를 길게 누르는 빠른 연속 이동에서도 좌표가 중앙이나 좌상단으로 초기화되지 않게 한다.
3. 메인 화면의 날짜별 검사 NG율 그래프가 카드의 가로 영역을 충분히 사용하도록 좌우 여백을 줄인다.
4. Position별 NG 카드는 한 줄에 2개씩 배치하고, 내부 지표 이름은 줄바꿈 없이 표시한다.
5. Position별 미검 Cell ID는 메인 왼쪽 열에서 Position별 NG 바로 아래에 배치한다.
6. Position별 Tool NG의 Threshold 입력값 `0.50`이 잘리지 않도록 입력 글자와 칸을 조정한다.
7. 분석 화면은 왼쪽에 필터와 4개 Score 지표, 오른쪽에 조건 CSV 저장·실제 NG 검출 최소·요약 통계를 위에서 아래 순서로 배치한다.
8. 분석 화면의 글자 크기를 다른 메뉴와 맞추고 화이트 모드 글자 대비를 높인다.
9. 시뮬레이션 실행 중에는 어느 메뉴에서도 하단 고정 진행률을 표시하고, 진행 화면으로 돌아가는 버튼을 제공한다.

## 원인과 수정 원칙

- 분류 화면의 빠른 이동 문제는 이전 이미지가 완전히 로드되기 전에 중간 렌더링 상태가 저장 좌표를 덮어쓰는 경쟁 상태였다.
- 좌표는 이미지 픽셀이 아닌 정규화된 중심 비율로 보존하고, 현재 이미지의 로드가 끝난 경우에만 사용자 스크롤 좌표를 갱신한다.
- 날짜별 그래프는 SVG viewBox 비율을 실제 카드의 가로·세로 비율에 맞춰 불필요한 레터박스 여백을 제거한다.
- 화면 배치는 가로 스크롤이나 내부 스크롤에 의존하지 않고 데스크톱에서 한눈에 비교할 수 있는 2열 구조를 우선한다.

## 완료 기준

- 정적 회귀 테스트와 Chromium 브라우저 회귀 테스트를 모두 통과한다.
- Web v4.7.13 / Local Agent v1.3.3으로 버전 정보를 일치시킨다.
- Agent 설치 파일을 다시 빌드하고 설치·등록·실행한다.
- Git 커밋·푸시와 GitHub Pages 배포 확인 후, Agent 연결과 Green Workspace 로드 상태까지 확인한다.
