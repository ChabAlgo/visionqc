# VisionQC 4.8.1 / Agent 1.4.1 작업·검증 기록

## 적용 범위
- 원본 CSV와 시뮬레이션 결과는 에이전트 SQLite에 유지. 브라우저는 집계, 미검 100건, Score 300건과 선택한 이미지 상세만 조회.
- 원본 쓰기와 집계 읽기는 동시에 진행. 집계 커서와 변경된 Cell/Position/Tool 카운터를 원자적으로 반영. 완료 시 처리 수와 원본 분석 수 일치 확인.
- 전체/날짜별/Tool/실제 NG/최종 미검 집계, Threshold, 원본 상세, 방향키 페이지 경계, 이미지 제외 후 갱신, CSV 출력, PDF 요약, DB 명시 저장 연결.
- CSV 다중 선택, 기본 100만 데이터 행 분할, 날짜별 분할, Date/Time 분리. 잘못된 후속 파일·취소는 논리 입력 전체 롤백.
- 분석 참조를 보관하여 새로고침·프로세스 재시작 후 복원. 입력 제거와 CSV 교체 시 제외한 Position을 다시 불러오지 않도록 처리.
- Class Presets JSON 백업/복원, 단축키 오류 팝업은 선행 커밋 7d84994d5380951cec43336bd8cfc046eaa65660을 포함.
- Windows TCP TIME_WAIT 때문에 종료 직후 재기동 시 포트 충돌 팝업에서 대기하던 문제 수정. 포트별 프로세스 mutex로 중복 실행을 막고 소켓 재사용으로 즉시 재시작 허용.

## 검증 근거 (2026-09-21, 이 PC)

### 대용량 네이티브 집계
- 2,000,000 Cells × 2 Positions × 4 Tools = 4,000,000 원본 행 / 16,000,000 Tool 결과.
- 전체 및 NG 수, 중복 재처리 방지, Score 조회, 실제 NG 1,000건 매칭 검증 통과.
- 최종 집계 생성: 645.092초. 생성 중 표본 Working Set 최대 110.637 MiB, summary 2.835ms.
- 준비된 캐시 조회: 대시보드 61.914ms, 전체 Tool0 통계 11,676.421ms. 실제 NG 1,000건 조건 대시보드 13.664ms, 해당 Score 통계 20.845ms.
- source DB 약 4.07 GiB, projection DB 약 2.43 GiB. 파일 경로·Tool 수에 따라 변동.
- 로그: C:/Temp/vq481-2m-current.log, C:/Temp/vq481-2m-views.log.
- 재사용 캐시 벤치마크의 peakWorkingMiB=0은 집계 생성 반복문이 실행되지 않아 측정하지 못한 값이다. 메모리 0으로 해석하지 않는다.

### 실제 GPU와 데이터 수명주기
- 사용자 허용 워크스페이스를 읽고, 원본 이미지 3장을 별도 임시 폴더에 복사하여 4 Position 병렬 검사. 실행당 12건, 버전별 3회.
- 기존 v1.3.36: 5.709 / 4.906 / 5.289초. v1.4.1 후보: 6.483 / 5.370 / 5.274초.
- 중앙값 5.289 → 5.370초(+1.52%). 작은 표본이며 다른 테스트 부하가 겹쳐 속도 동일/향상 또는 퇴보를 확정할 수 없다. GPU 추론·전처리 알고리즘 자체는 변경하지 않았다.
- 마지막 실행의 48개 Tool 판정은 모두 일치. Score 최대 절대 차이 0.000044(0.0044%p). 비트 단위 동일 또는 VPDL UI와 동일하다는 주장은 하지 않는다.
- 완료 결과 12건 집계, CSV 2행 제한/날짜 분할 출력, 전부 재입력, 명시 DB 저장 두 번의 중복 방지 검증 통과.
- 초기 수명주기 테스트의 프로세스 재시작은 포트 TIME_WAIT 문제로 실패했다. 수정 후 같은 결과를 이용한 즉시 재시작 3회, 각 회차 12건 복원 및 중복 프로세스 차단을 별도 검증했다.
- 로그: C:/Temp/vq481-release-native.log (초기 재시작 실패 포함), C:/Temp/vq481-restart.log (수정 후 3회 PASS).
- 재시작 fixture: C:/Users/HP/AppData/Local/Temp/VisionQC-481-candidate-2l4fy9/restart-report.json.
- 기존 사용자 Agent 17891과 별도로 17931 포트를 사용했다. 사용자 이미지·워크스페이스·운영 DB는 테스트 중 수정하지 않았다.

### 회귀 검사
- 정적 테스트 144/144 통과: C:/Temp/vq481-final-static.log.
- 전체 브라우저 77/77 통과 후 실시간 참조 복원 테스트 추가. 최종 전체 78/78 통과: C:/Temp/vq481-final-browser.log.
- Blue 단독 완료 보존을 추가한 최종 Agent 전용 브라우저 검사 6/6 통과. 로그: C:/Temp/vq481-final-live-browser.log. 원본 400만 행을 브라우저에 주입한 검사가 아니라, 같은 총량을 나타내는 모의 API 응답으로 페이지 크기 제한과 탐색/복원 프로토콜을 검증했다.
- 네이티브 CSV/SQLite 정합성 검사 통과: C:/Temp/vq481-release-integrity.log, C:/Temp/vq481-release-data.log. Score 정밀도, 날짜, Workspace, 다중 파일 오류/취소, 증분 커서, 최종 Cell 미검, 같은 Score 순서, 역방향 페이지, 위치 제거, 전체/조건별 CSV 재입력 포함.
- 1만 개 합성 PNG 파일 로드 + 30회 방향키 이동: 중앙값 35~38ms, p95 48~54ms. 가상화 및 기존 이미지 캐시 경로는 유지했다. 다른 실제 원본 1만 장 또는 네트워크 드라이브 검사는 아니다.
- Worker 빌드: API 8.0, 8.2, Universal, Core 성공. 실제 라이선스/GPU 검사는 설치된 API 8.0만 수행.
- 실제 NG 파일 참조, 분류 라벨/썸네일, VPDL 모델·작업 목록은 계속 각 기능의 메모리를 사용한다. 전체 PC 메모리가 110MiB라는 주장은 하지 않는다.

## 배포 산출물과 제한
- downloads/VisionQC_Agent_Installer_v1.4.1.exe
- downloads/VisionQC_Offline_v4.8.1.zip
- RELEASE_MANIFEST.json에 각 SHA-256 기록. release-artifact-audit.ps1로 설치 파일 내부 리소스가 최종 소스/Worker와 일치하는지 확인한다.
- 이미지/워크스페이스/Cognex DLL/사용자 DB/테스트 로그는 설치 파일에 포함하지 않는다.
- 200만 실제 이미지의 GPU 장시간 검사·200만 Cell 전체 CSV 입출력 수명주기는 수행하지 않았다. 합성 집계 규모 검사, 작은 실제 GPU 검사, 모의 브라우저 프로토콜 검사를 구분한다.
- 대량 미검 5,000건 초과의 PDF는 전체 수량 요약과 CSV 전체 목록 안내를 제공한다. Excel 브라우저 호환 입력은 50MB 이하이며 대용량 입력은 Agent CSV 경로다.
- 임시 분석/복원 자료는 디스크에 보존하므로 반복 입력 시 디스크 사용량이 증가한다. 사용자 저장 이력과 다른 위치에 있으며 이번 배포는 기존 분석 자료를 자동 삭제하지 않는다.
