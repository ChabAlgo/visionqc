# VisionQC 4.7.26 / Agent 1.3.16

## 변경 내용

- 설정 → 분석 Input → 파일명 규칙에 `JSON 내보내기`와 `JSON 가져오기`를 추가했습니다.
- 내보내기는 화면에 입력된 현재 규칙을 `visionqc-naming-profile` 형식, 스키마 버전 1의 UTF-8 JSON 파일로 저장합니다.
- 가져오기는 파일 형식·스키마 버전·규칙 이름·구분자·추출 방식·토큰 번호·Cell ID 길이를 확인한 뒤 현재 규칙에 적용합니다.
- 잘못된 JSON, 지원하지 않는 스키마, 필수 필드 누락, 범위를 벗어난 값은 현재 규칙을 바꾸지 않고 오류로 안내합니다.
- 가져온 규칙은 현재 규칙으로 유지됩니다. 이름별 저장 목록에 추가하려면 기존 `규칙 저장`을 누릅니다.

## 기존 기능 보존

- 파일명 파서, Agent 미리보기 API, Cell ID·촬영 날짜시간 추출 방식은 변경하지 않았습니다.
- Green·Blue·Red·Integrated 검사, Workspace, ROI, Tool, Threshold, 판정, CSV/SQLite, Source/Crop/Overlay 처리 코드는 변경하지 않았습니다.
- 기존 브라우저 저장 규칙과 이름별 저장/불러오기 동작을 유지합니다.
- Agent 1.3.16은 4.7.26 오프라인 웹 화면을 설치 파일에 포함하기 위한 배포 버전이며 검사 엔진 변경은 없습니다.

## 사용 방법

1. 규칙 값을 입력한 뒤 `JSON 내보내기`를 누르면 현재 화면의 규칙을 파일로 받습니다.
2. 다른 PC에서 `JSON 가져오기`를 누르고 해당 파일을 선택합니다.
3. 화면 값과 필요 시 Agent 미리보기를 확인합니다.
4. 이름별 저장 목록에도 보관하려면 `규칙 저장`을 누릅니다.

## 검증

- 내보낸 JSON의 형식·스키마·규칙 값 및 파일명을 브라우저에서 확인했습니다.
- 정상 JSON 가져오기 후 화면과 브라우저 현재 규칙이 일치하고, 이름별 저장 목록은 자동 변경되지 않음을 확인했습니다.
- 지원하지 않는 스키마 파일이 오류를 표시하고 직전 규칙을 유지함을 확인했습니다.
- 정적 회귀 125개와 브라우저 회귀 34개가 모두 통과했습니다.
- Agent/Worker/Installer 빌드가 완료됐으며 배포 산출물 검사에서 내장 리소스 17개와 API 8.0·8.2·Universal·Core 구성을 확인했습니다.
- 오프라인 ZIP은 설치 파일·오프라인 안내·릴리스 노트·버전 파일 4개만 포함하고 이미지·Workspace·DB·로그·Cognex DLL·비교 실행기를 포함하지 않습니다.

## 검증 경계

- 파일명 규칙 전송 외의 검사 기능은 이번 변경에서 수정하지 않았습니다.
- 실제 H100·Red 모델·장시간 VPDL 추론은 이 웹 기능의 변경 범위가 아니며, 이전 릴리스의 현장 검증 경계를 그대로 유지합니다.
