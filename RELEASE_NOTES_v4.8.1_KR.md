# VisionQC 4.8.1 / Agent 1.4.1 변경 내역

## 대용량 분석
- CSV 여러 개를 선택해 하나의 분석으로 읽습니다. 에이전트가 순차로 읽어 SQLite에 저장하며, 브라우저에는 원본 전체 대신 집계와 현재 페이지의 데이터만 전달합니다.
- 실시간 시뮬레이션도 같은 경로를 사용합니다. 새 결과만 증분 반영하고, 완료 시 처리 수와 분석 원본 수를 대조합니다. 분석 커서와 집계는 함께 커밋하여 재시도 시 중복 집계를 방지합니다.
- 미검 목록은 100건, Score 그래프는 300건씩 조회합니다. 이전/다음 페이지와 이미지 방향키 이동으로 전체 결과를 탐색하며, 통계와 히스토그램은 전체 대상 기준입니다.
- Threshold, 날짜 선택, 최종 Cell·Position 미검 판정, 다른 Tool NG 제외 조건을 에이전트에서 계산합니다. 같은 날짜 + Cell ID + Position을 합치며 날짜가 다르면 별도 검사로 집계합니다.
- 분석 참조를 저장하여 새로고침·에이전트 재시작 후 복원합니다. 원본 로그와 임시 분석은 사용자가 승인한 영구 검사 이력과 구분됩니다. No 이후에도 메인 상단에서 DB 저장할 수 있습니다.
- 잘못된 후속 CSV나 취소로 전체 입력에 실패하면 일부 결과를 성공한 분석으로 표시하지 않습니다. 기존 분석을 유지합니다.

## CSV 출력과 날짜·시간
- Simulation Output에서 파일당 최대 데이터 행 수를 지정합니다. 기본값은 1,000,000행이며 제목 행은 별도입니다.
- ‘날짜별 분할’을 함께 사용할 수 있습니다. 날짜 미확인 결과도 별도 파일에 보존하고 각 파일에 제목 행을 반복합니다.
- 따옴표 안 쉼표·줄바꿈도 한 레코드로 처리합니다. 분할 안내 파일은 `.parts.txt`이며 사용자가 읽을 수 있는 CSV가 실제 결과입니다.
- 촬영 시각은 `Date` / `Time` 열로 분리합니다. 예: `2026-08-07` / `01:20:30`. CSV에는 시트가 없으므로 같은 행의 두 열입니다.
- 처리 시각은 `InspectionDate` / `InspectionTime`으로 구분합니다. 이전 CaptureTimestamp 형식도 읽습니다.
- 전체 결과·조건별 Score·미검 CSV는 화면 페이지와 관계없이 전체 대상에서 저장합니다. 원본 Score 정밀도, Position, Workspace, 경로를 유지합니다.

## 분류와 리포트
- Class Settings의 Class Presets를 JSON으로 내려받고 다시 가져올 수 있습니다. 잘못된 파일·취소·저장 오류 시 기존 설정을 유지합니다. 단축키 중복 오류는 팝업으로 표시합니다.
- 이미지 분류의 기존 라벨·방향키·줌·파일 처리 기능을 유지합니다. 대용량 분석 데이터를 분류 화면의 브라우저 메모리에 중복 적재하지 않습니다.
- 요약 리포트의 수량·비율은 전체 결과입니다. 미검 5,000건 초과 시 PDF에 수백만 개의 텍스트를 생성하는 대신 전체 미검 CSV 저장 안내를 표시합니다. 5,000건 이하는 기존처럼 ID 목록을 제공합니다.

## 검증과 사용 범위
- 200만 Cell × 2 Position × 4 Tool 합성 데이터: 원본 400만 행, Tool 결과 1,600만 건의 전체/NG 집계와 재처리 중복 방지 검증 통과.
- 신규 집계 생성 645.09초, 집계 생성 중 표본 측정 프로세스 메모리 최대 110.64 MiB. 준비된 데이터의 대시보드 조회 약 62ms. 전체 Tool 통계 조회는 약 11.7초로 별도 처리됩니다.
- 위 메모리는 집계 프로세스 측정치이며 PC 전체·GPU·이미지 분류 메모리가 아닙니다. 실제 200만 장 GPU 검사 시간이나 모든 PC에서 동일 성능을 보장하는 수치가 아닙니다.
- 1만 개 합성 이미지가 로드된 분류 화면의 30회 방향키 이동: 중앙값 35~38ms, p95 48~54ms. 실제 네트워크 드라이브의 서로 다른 고해상도 이미지에 대한 성능 보장은 아닙니다.
- SQLite는 디스크를 사용합니다. 이번 400만 행 예제에서 원본 약 4.07 GiB, 분석 캐시 약 2.43 GiB를 사용했습니다. 실제 용량은 경로 길이·Tool 수·이력에 따라 달라집니다.
- 실제 NG 폴더의 파일 참조, 분류 라벨·썸네일, VPDL 내부 모델·작업 목록은 각 기능에서 계속 관리합니다. 모든 종류의 메모리 사용이 일정하다는 의미는 아닙니다.
- 대용량 CSV는 Agent 1.4.1 경로를 사용하세요. Excel 직접 입력은 기존 브라우저 호환 경로로 제공하며 50MB 이하입니다. 더 큰 Excel은 CSV로 저장하여 여러 파일을 선택하세요.

정적·브라우저·네이티브 검사 및 배포 산출물 검증 상세는 WORKLOG_v4.8.1.md에 기록합니다.
