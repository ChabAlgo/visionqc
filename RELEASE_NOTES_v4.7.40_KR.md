# VisionQC 4.7.40 / Local Agent 1.3.27

## 데이터 정합성 보완

- Score 그래프의 각 점이 해당 원본 검사 행의 이미지, Score 및 Heatmap에 연결됩니다. 같은 날짜·Cell·Position의 다른 이미지 값을 가져오지 않습니다.
- 전체 결과 CSV는 원본 검사 행을 모두 저장합니다. 대표값을 첫 이미지 경로와 결합하던 동작을 제거했으며 Score 정밀도, 촬영 시각, Workspace 정보를 보존합니다. 화면의 날짜·Cell·Position 집계 방식은 유지합니다.
- 병렬 Position CSV는 열 이름으로 합칩니다. Tool 개수·순서 차이, 따옴표, 줄바꿈을 처리하며 잘못된 열 수, 중복 헤더 또는 누락된 파일은 오류로 알립니다. 병합이 끝난 파일만 최종 결과로 게시합니다.
- CSV/SQLite 가져오기는 잘못된 Score와 손상된 행을 거부합니다. 실패한 가져오기의 일부 행이 정상 이력으로 남지 않으며 기존 이력은 유지됩니다.
- Windows 소수점 형식과 무관하게 Score를 저장합니다. 이전 소수점 쉼표 CSV도 읽을 수 있습니다.
- CSV 화면 저장과 실시간 DB 동기화에서 촬영 시각·Workspace 정보를 유지합니다.
- 실제 NG 폴더에서 같은 Cell의 모든 날짜·이미지를 색인합니다. 원본 이미지 바이트는 열 때만 읽습니다.
- Workspace 내용이 바뀌면 이전 Runtime 재사용을 거부하고 Runtime File Load를 요청합니다. 실행 중 변경된 파일을 새로 로드한 것처럼 기록하지 않습니다. 내용 확인은 로드/시작/종료 경계에서 수행하며 개별 이미지 추론에는 추가하지 않습니다.
- Heatmap은 검사 View의 회전·확대·소수점 위치를 반영합니다. 이미지 밖 영역은 늘려 그리지 않고 잘라 표시합니다. 검사 ROI와 모델 Score 계산은 변경하지 않았습니다.

## 검증 및 적용

자동 회귀 검사, 20만 행 증분 집계, 원본 CSV 재저장/재입력, 별도 임시 SQLite의 실패 가져오기 취소, CSV/ROI/Workspace 변경 네이티브 테스트와 x64 빌드·배포물 검사를 수행합니다.

Agent 변경은 새 설치 프로그램 실행 후 적용됩니다. 사용자 이미지·Workspace·기존 DB를 테스트로 변경하거나 실행 중 Agent를 자동 교체하지 않습니다.

VPDL Studio와 VisionQC Score의 직접 대조는 사용자가 새 Workspace와 Test 이미지를 제공한 뒤 별도로 진행합니다. 이번 검증은 Studio와의 Score 일치를 보증하지 않습니다.

### 2026-09-17 검증 결과

- 정적 회귀 검사 144개, Chromium 브라우저 회귀 검사 55개 통과. 20만 행 증분 집계 및 전체 재집계 일치 검사 포함.
- C# 정합성 테스트 통과: Tool 열 순서/개수 차이, 멀티라인 CSV, 손상 행, 소수점 문화권, 원자적 병합, 회전/소수점/경계 ROI, 동일 크기·수정 시각의 Workspace 내용 변경.
- 별도 임시 SQLite 테스트 통과: Score 정밀도·Workspace·실시간 조회 정보 보존, 401행 입력 후 실패 시 부분 저장 취소 및 기존 이력 유지.
- 기존 네이티브 테스트 통과: 파일명/촬영 시각 25건, Runtime 정책 11건, 처리 프로세스 통신/취소/오류 격리 26건.
- x64 Agent와 API 8.0/8.2/Universal/Core 구성 빌드 성공.

이미 대표값 한 행으로 축약되거나 잘못된 Tool 열에 저장된 과거 CSV는 없는 원본 값을 복구할 수 없습니다. 해당 파일은 원본 Position CSV 또는 원본 시뮬레이션 로그로 다시 불러와야 합니다.
