VisionQC Web v4.4.25 정적/회귀 검증
===================================

PASS
- Web static regression: 12/12 PASS
- visionqc-extension.js: node --check
- assets/index-v4.4.25.js: node --check
- tests/v4425-regression.test.mjs 실행
- index.html v4.4.25 asset/version 참조 확인
- v4425 stylesheet가 legacy extension stylesheet 뒤에 로드됨
- expanded drawer가 shell/classification content를 272px 기준으로 이동
- Simulation Options fixed outer + stateful inner scroller 분리 확인
- 외부 pointer/click scroll guard가 preventDefault/stopPropagation을 사용하지 않음
- Agent 상태 갱신이 전체 Simulation DOM을 교체하지 않음
- Workspace 응답이 대상 summary/select만 갱신
- Tool Settings fixed table layout / min-width:0 / horizontal overflow 차단 확인
- Fallback 980px table 제거 및 card layout 확인
- Preview modal horizontal overflow 차단 확인
- Simulation native text selection 허용 확인
- 1439px 이하 1열 responsive fallback 확인
- authoritative stylesheet brace balance 확인
- Local Agent v0.2.4 static regression 실행
- Workspace Inspect fallback의 잘못된 OptimizedGPUMemory(GPU index) 호출 제거 확인
- Agent CMD 9개 모두 UTF-8 BOM 없음 / 첫 줄 @echo off / ASCII-safe 확인
- Agent 단일 Control 재사용/직렬화/Simulation ownership/cache 확인
- Agent csproj의 Compile Include 파일 존재 및 0.2.4 버전 정합성 확인
- Local Agent static regression: 9/9 PASS

직접 원인 확인
- body[data-vq-page="simulation"]는 CSS용 현재 화면 상태인데, 전역 click 위임이 closest('[data-vq-page]')로 body까지 navigation으로 인식
- Simulation 내부 어디를 클릭해도 setPage('simulation')이 재호출되어 전체 DOM 교체, text selection 해제, Options scroll reset이 동시에 발생
- selector를 .vq43-nav-item[data-vq-page]로 제한하고 회귀검사에서 같은 Options DOM node가 유지되는지도 확인

브라우저 현재 배포본 v4.4.23 재현
- Options를 wheel로 scroll한 뒤 Main 빈 영역 클릭
- Options DOM outerHTML 변화 없음
- click 직후 scrollTop이 520 -> 0으로 초기화되는 현상 확인
- v4.4.25에서는 fixed outer + stateful inner scroller와 외부 click guard 적용

제작환경 제한
- Chromium 1920x1080 실제 mouse drag/mouseup/click/overflow Playwright spec 및 GitHub Actions workflow 포함
- 현재 저장소 쓰기 권한이 연결되지 않아 이 환경에서 branch push/Actions 실행은 불가. 사용자가 commit/push하면 workflow가 자동 실행됨
- 현재 작업 환경에는 Windows .NET Framework/MSBuild 및 Cognex VPDL 4.0 DLL이 없어 Agent 실제 Rebuild/Runtime 검증은 VPDL PC에서 수행 필요
- 사용자 제공 Windows 빌드 로그는 수정 전 전체 C# 컴파일에서 CS1503 1건만 보고했으며, 해당 호출은 제거함
