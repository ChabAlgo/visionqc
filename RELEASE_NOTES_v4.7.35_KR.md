# VisionQC v4.7.35 / Agent v1.3.22

## 수정 내용

Position Worker 진행 수와 브라우저가 받은 SSE 상세 행 수가 크게 벌어져 실행 중 대시보드가 실제 처리량보다 적게 보이던 현상을 개선했습니다.

Web은 약 2초마다 현재 Simulation 실행 ID와 마지막 SQLite `image_id`를 기준으로 새 결과만 요청합니다. Agent는 현재 기록 트랜잭션을 짧게 반영한 뒤 새 행만 반환합니다. DB 동기화가 활성화된 동안 SSE 상세 행은 화면 데이터에 중복 추가하지 않습니다.

완료 시에는 v4.7.34에서 추가한 전체 결과 재검증도 그대로 수행합니다. 따라서 실행 중에는 최대 약 2초 정도의 지연으로 따라가고, 완료 후에는 저장 건수와 정확히 일치합니다.

## 안전 장치

- 다른 실행 ID 결과는 반환하지 않습니다.
- 마지막으로 반영한 `image_id` 이후 행만 가져옵니다.
- 실시간 DB 동기화와 완료 전체 동기화를 분리해 완료 데이터가 중복되지 않습니다.
- DB 동기화 오류는 검사 실행을 중단시키지 않고 다음 상태 확인에서 다시 시도합니다.

## 확인

- 정적 회귀 테스트: 139개 통과
- 실시간 DB 증분 및 완료 전체 동기화 브라우저 회귀: 2개 통과
- 전체 브라우저 회귀: 46개 통과
- Windows x64 Release 빌드: 오류 0개
- 배포 패키지 리소스 및 SHA-256 감사: 통과

## 적용 조건

Web v4.7.35와 Agent v1.3.22를 함께 사용해야 실행 중 DB 증분 동기화가 동작합니다.
