# VisionQC 4.7.25 / Agent 1.3.15

## 변경 내용

- 독립 Green에서 해결된 시스템 우선 DLL 검색 정책을 공통 Worker(사전 로드, AI SUGGEST, Blue, Red, 통합 검사)에도 적용했습니다. 선택한 VPDL의 관리 SDK/API 매칭은 유지합니다.
- AI SUGGEST는 외부 생성형 AI나 API 키 없이 로컬 VPDL을 직접 호출합니다. 시뮬레이션 종료 후 해제된 런타임도 사용자가 AI 검사를 누르면 다시 준비합니다. 활성 Position이 파일명에 있어야 하며, 그 Position의 Green/Red Workspace가 설정되어야 합니다. Blue만 설정한 상태에서는 분류 모델이 필요하다는 안내를 냅니다.
- Green은 기존 BestTag/Score 판정을 유지합니다. Red는 Green 태그로 잘못 읽지 않고 네이티브 Regions/Score/처리 오류를 읽습니다. Red Workspace 임계값/필터를 변경하지 않습니다. Blue 검출·Crop 및 통합 원본/Crop 경로 구분을 유지합니다.
- 테스트용 비교 실행기와 바탕화면 비교 바로가기를 배포에서 제외했습니다. 설치 프로그램이 자기 소유임을 확인한 기존 비교 바로가기만 제거합니다. 이전 릴리스, 개발용 회귀 테스트, 사용자 DB·이미지·Workspace·로그는 보존합니다.
- 실험용 TensorRT/메모리/새 Runtime/비교 옵션은 화면에서 제거하고 기존에 저장된 실험 설정도 운영 기본값으로 정규화합니다. GPU 번호·툴·ROI·저장 옵션은 유지합니다. 정상 검사 상세 로그는 줄이고 실패 단계/원인은 계속 기록합니다.

## 파일명 날짜 규칙

설정 → 분석 Input의 파일명 규칙에서 선택합니다.

1. YYYYMMDDHHMMSS: 예를 들어 20260824074705.
2. YYYYMMDD_HHMMSS: 예를 들어 20260824_074705.
3. 구분자 기준 위치 지정: 지정 토큰의 14자리 날짜시간, 또는 연속된 8자리 날짜와 6자리 시간.

토큰 번호는 위치 지정일 때만 활성화됩니다. 날짜는 파일명만 읽고 폴더 숫자·셀 ID 일부는 날짜로 추측하지 않습니다. 존재하지 않는 날짜, 중복 후보는 날짜 미확인으로 남깁니다. 기존 비연속 날짜/시간 토큰 규칙은 호환 목적으로 보존합니다.

CSV에 명시적인 CaptureTimestamp가 있으면 이를 사용합니다. 원본 FullPath가 있는 일반 시뮬레이션 CSV는 검사 실행일 대신 그 파일명의 촬영 시각을 읽습니다. 시간대 변환으로 하루가 바뀌지 않도록 촬영 날짜를 그대로 표시합니다.

규칙 이름을 입력하고 규칙 저장 → 저장된 규칙 선택 → 규칙 불러오기로 전환합니다. 규칙은 현재 브라우저에 저장되며 다른 PC로 자동 동기화되지는 않습니다.

## 날짜별 대시보드

메인 날짜별 NG율의 날짜 영역을 누르면 해당 날짜의 원본 행을 먼저 선택한 뒤 Cell/Position/Tool/미검을 다시 계산합니다. 전체보기로 전체 분석 결과를 복원합니다. 날짜 그래프는 날짜 전환을 위해 전체 기간을 유지하며 선택일을 강조합니다. 다른 분석/검사 이력 페이지의 원본 결과는 삭제하거나 변경하지 않습니다.

## 원인과 검증 경계

보안 서버 비교에서 기존/현재 엔진 모두 원래 DLL 검색은 통과하고 Cognex bin 고정 검색은 실패했습니다. 4.7.24 독립 Green 수정 후 사용자도 서버 시뮬레이션 성공을 확인했습니다. 핵심은 SDK 폴더를 Windows 시스템 DLL보다 먼저 검색하게 한 로딩 정책입니다. 오류 코드 6cc4a157만으로 메모리 부족을 확정하지 않습니다. 서버에서 정확히 어느 네이티브 DLL이 충돌했는지는 전체 모듈 비교 없이는 단정하지 않습니다.

로컬 테스트는 RTX 4050, VPDL 4.0 및 4.2 환경입니다. H100 보안 서버에 직접 접속하지 않습니다. 학습된 Red Workspace가 로컬 검증 자료에 없어 Red 실모델의 판정/히트맵 일치는 현장 확인이 필요합니다. 다른 기능의 테스트 통과를 Red 실검사 통과로 표현하지 않습니다.

## 최종 검증

- 정적 회귀 125개, 브라우저 33개, C# 날짜/로그/정책/프로세스/비교 테스트 100개 통과.
- VPDL 4.0 및 4.2: 독립 Green 각 6건, Tool 24건, Overlay 8개. 각 환경에서 AI SUGGEST 2회 연속 성공, 잘못된 이미지 실패, SQLite 검사 이력 변화 없음.
- VPDL 4.0 Blue: 6개 Crop 저장, Error 0. 통합: 6건/Tool 24건, 오류 없음. Universal Worker + 4.0: 6건/Tool 24건/Overlay 8개.
- 원본 비교: 최초 Agent 실행과 원본은 24개 Score 중 최대 0.000898 차이가 있었고 판정/Cell ID/Judgement 차이는 0건입니다. 기존 엄격 Score 허용치 0.0001 테스트는 통과로 처리하지 않았습니다.
- 추가 반복 비교(각 24 Score): 원본 재실행 최대 0.000088, 이전 배포본 0.000062, 현재 독립 실행 0.000088, 현재 재실행 0.000013. 모두 판정 변경 0건. 미세 Score 차이가 완전히 재현/해소됐다고 주장하지 않으며, 판정 임계값에 근접한 생산 데이터는 현장 확인이 필요합니다.
- 파일명 날짜 파서: 실제 8/24~9/3 형식, 잘못된 달/윤일/시간, 셀 ID 내부 숫자, 폴더 숫자, 중복 후보, 토큰 위치, 기존 분리 규칙을 검증했습니다.
- 브라우저: 실제 AI 버튼의 로컬 요청·실패 안내, CSV 촬영일 우선, 날짜 클릭/키보드 선택/전체보기, 중복 Cell의 날짜별 분리, 이름별 규칙 저장/불러오기, 기존 뷰어/레이아웃 검증.
- 패키지에는 검사 이미지·Workspace·DB·로그·Cognex DLL을 포함하지 않습니다. 설치 파일 리소스 해시와 Worker 구성은 release-artifact-audit.ps1로 검사합니다.

## 배포 후 확인

Agent 1.3.15를 설치하고 Web 4.7.25인지 확인합니다. 보안 서버에서는 이전과 같은 Green 설정으로 시작한 뒤 Blue/통합/Red는 각 실제 Workspace와 이미지로 순서대로 확인하세요. 날짜 규칙을 선택한 후 과거 CSV도 다시 읽으면 검사 실행일 대신 원본 파일명 촬영일을 사용합니다. 기존 저장된 SQLite 이력은 이번 작업에서 자동 재작성하지 않습니다.
