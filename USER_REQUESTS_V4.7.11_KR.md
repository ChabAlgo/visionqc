# v4.7.11 사용자 요청 및 유지보수 기록

## 반영 요청

- 이미지 분류의 빠른 이동 시 확대 여부와 무관하게 초점이 이동하는 문제를 제거한다.
- 분석 드롭다운을 연 상태에서도 실시간 결과·그래프는 계속 갱신한다.
- 메인 화면은 Cell별 NG를 좌측, 날짜별 NG율을 우측에 배치한다.
- Position·Tool 카드의 내부 스크롤을 제거하고 Threshold 입력이 잘리지 않게 축소한다.
- 다크 모드 날짜 입력의 달력 아이콘을 확실히 표시한다.
- 이미지 Viewer는 상·좌 이전, 하·우 다음으로 이동하며 확대·초점을 유지한다.
- SQLite에 검사모드, Workspace, Web 버전을 저장하고 구버전 빈 모드는 실행 정보로 보완한다.
- VPDL Worker 선택기는 동일 테마로 정리하고 DLL 버전 충돌을 막는 목적을 화면에 설명한다.

## 구조 및 제한

- 분류 화면 초점 보존은 원본 React Viewer의 이미지 ID 세대·비율 기반 로직만 사용한다. 확장 코드에서 별도 load 타이머로 스크롤을 덮어쓰지 않는다.
- 구버전 DB에서 Workspace 경로 자체가 저장되지 않은 행은 실제 파일을 추측해 채우지 않는다. 검사모드는 runs.mode로 복구하며 새 Simulation은 Position별 Workspace 이름·키를 계속 저장한다.
- VPDL Worker는 설치된 VPDL API별 DLL을 별도 프로세스로 격리하여 한 Agent에서 여러 VPDL 버전을 안전하게 선택하기 위한 구성이다.
