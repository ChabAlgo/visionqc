# VisionQC v4.7.12 사용자 요청 및 유지보수 기록

## 반영 요청

- 이미지 Viewer의 100% 상태는 화면 맞춤 기준으로 원본 전체가 보여야 한다.
- Heatmap을 보고 있을 때도 키보드 방향키로 이전/다음 Heatmap 이미지를 이동한다.
- 날짜별 NG율 그래프의 좌우 여백을 줄이고 한 날짜만 있어도 선이 영역 전체를 사용한다.
- 메인 화면의 Cell별 NG와 Position별 NG 사이 여백을 없애고 요약 문구가 잘리지 않게 줄을 명시한다.
- Position별 Tool NG 구성은 한 행에 Position 두 개를 배치한다.
- 분석 화면의 조건/저장 영역을 두 칸으로 묶어 넓은 화면을 효율적으로 사용한다.
- 분류 화면에서 빠르게 이미지를 넘길 때 이전 이미지의 늦은 load 이벤트가 현재 좌표를 덮어쓰지 못하게 한다.
- SQLite 이력 자동 조회 실패 뒤 무한 재예약하지 않는다. 자동 조회 실패는 한 번만 기록하고 사용자가 조회 버튼으로 재시도한다.
- 브라우저가 시간 초과/새로고침으로 loopback 연결을 닫은 정상 상황은 Agent 오류 알람으로 표시하지 않는다.

## 유지보수 제한사항

- 분류 화면은 기존 React 번들 내부의 object URL과 이미지 ID 소유 관계를 검증한다. 번들 교체 시 v4.7.12 회귀 테스트를 반드시 실행한다.
- 실제 VPDL Runtime/라이선스/GPU 검사는 Windows Agent 설치본으로 별도 확인한다.

## 기본 완료 기준

- VisionQC 수정 작업은 코드 반영과 테스트만으로 종료하지 않는다.
- 웹 버전과 Agent 버전을 갱신하고 설치 파일을 다시 빌드한다.
- 전체 회귀 테스트와 `git diff --check` 통과 후 변경 내용을 커밋하고 GitHub `main`에 푸시한다.
- GitHub Pages 배포본에서 새 버전과 핵심 변경을 확인한다.
- Agent를 재설치하고 프로토콜을 등록한 뒤 백그라운드에서 실행한다.
- 브라우저에 저장된 Workspace 경로가 유효하면 Runtime File Load까지 실행하고 상태를 확인한다.
- 경로, 라이선스 또는 실제 VPDL 파일 문제로 Workspace를 로드할 수 없을 때만 정확한 차단 원인을 보고한다.
