# Class Presets 파일 백업과 복원

Class Settings의 Class Presets 영역에서 **JSON 내보내기**와 **JSON 가져오기**를 사용할 수 있습니다.

## 파일 백업

- JSON 내보내기는 Class 1~5에 저장한 프리셋과 현재 설정 창에서 편집 중인 값을 한 파일에 저장합니다.
- 라벨 ID·이름·종류·단축키·활성 여부·색상, Multi Label Key, 현재 이미지 라벨 해제 키가 포함됩니다.
- 현재 설정을 슬롯에 저장하지 않았더라도 파일에는 현재 편집값이 포함됩니다.
- 단축키 중복 등 잘못된 설정은 팝업으로 알려주며 내보내기를 중단합니다.
- 브라우저 저장 공간이 부족하더라도 유효한 현재 설정의 JSON 다운로드는 사용할 수 있습니다.

## 파일 복원

1. Class Settings → JSON 가져오기에서 백업 파일을 선택합니다.
2. 파일 검증 후 Class 1~5 교체 확인 창이 나타납니다. 취소하면 기존 설정을 유지합니다.
3. 확인하면 파일의 프리셋을 브라우저에 저장하고, 파일의 현재 설정을 편집 화면에 불러옵니다.
4. 실제 이미지 분류에 적용하려면 설정 창 하단 **Save**를 누릅니다. 특정 슬롯을 적용하려면 해당 슬롯의 Load 후 Save를 누릅니다.

가져오기는 백업 전체 복원이므로 파일에 없는 슬롯은 빈 슬롯으로 복원됩니다. 이미지 파일과 이미지별 라벨 결과는 이 JSON에 포함되지 않습니다.

잘못된 JSON, 지원하지 않는 형식·버전, 클래스 ID 중복, 잘못된 단축키나 단축키 중복이 있는 파일은 거부합니다. 파일 검증 또는 브라우저 저장에 실패하면 기존 프리셋과 편집값을 유지합니다. 설정 파일 크기는 최대 1MB입니다.

## 브라우저 저장에 관하여

프리셋은 현재 브라우저의 사이트 저장소에 보관됩니다. 접속 주소·포트·브라우저 프로필이 바뀌거나 사이트 데이터를 삭제하면 기존 프리셋이 보이지 않을 수 있습니다. 이번 변경은 별도 JSON 백업을 제공하며, 기존 설정이 사라진 정확한 원인을 확인한 것은 아닙니다.

## 검증 범위

- 브라우저 저장값 삭제 후 JSON 복원, 프리셋·라벨 해제 키·한글 이름 보존.
- 잘못된 파일, 단축키 중복, 가져오기 취소, 저장 공간 부족 시 기존 설정 보존.
- 브라우저 저장 실패 후에도 JSON 파일 다운로드 가능.
- 하단 Save의 단축키 중복 오류 팝업과 기존 이미지 라벨 해제 동작.

이 변경은 Class Settings 웹 기능 추가입니다. 별도로 진행 중인 4.8.1 대용량 분석 전환 및 Agent/오프라인 패키지 배포 완료를 의미하지 않습니다.
