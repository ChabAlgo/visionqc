# VisionQC v4.7.37 / Agent v1.3.24

## 수정 내용

Simulation 결과가 들어올 때마다 메인 대시보드와 Score 분석 화면의 전체 HTML을 다시 만들던 동작을 제한했습니다. 진행률 숫자와 막대는 기존 이벤트마다 즉시 갱신하고, 무거운 차트와 카드만 별도 주기로 갱신합니다.

대시보드 갱신 간격은 누적 결과 수에 따라 자동으로 조절됩니다.

- 10,000건 미만: 3초
- 10,000~49,999건: 5초
- 50,000~199,999건: 8초
- 200,000건 이상: 12초

마우스로 화면을 조작하거나 Score 드롭다운을 연 상태에서는 갱신을 미루며, 조작이 끝난 뒤 반영합니다. Simulation 완료 시에는 지연 중인 갱신보다 최종 SQLite 재동기화 결과를 우선해 즉시 다시 그립니다.

날짜별 NG율은 실행 중 들어온 새 행만 날짜별 누적값에 반영합니다. 같은 날짜의 동일 Cell ID와 Position이 다시 들어오면 기존 판정을 제거하고 합친 판정을 다시 반영하므로 전체 재계산과 같은 결과를 유지합니다. 날짜를 인식하지 못한 행 수도 증분 집계합니다.

## 정확성 및 성능 보호

- Cell·Position·Tool·실제 NG·미검 집계 규칙은 변경하지 않았습니다.
- 진행률 DOM 갱신은 대시보드 갱신 제한과 독립적으로 실행합니다.
- 완료 시 Agent 처리 건수, SQLite 저장 건수, 조회 건수와 화면 변환 건수 일치 검사를 유지합니다.
- 완료 전체 재동기화는 증분 상태를 버리고 SQLite 원본으로 다시 구성합니다.
- 200,000행, 25행 단위 합성 누적 시험에서 전체 행 수와 날짜별 누적값을 확인했습니다.

## 검증

- 증분 집계와 전체 재계산 동등성 및 날짜별 중복 병합 브라우저 회귀 통과
- 200,000행 증분 누적 브라우저 성능 회귀 통과
- 정적 회귀 테스트 통과
- 전체 브라우저 회귀 테스트 통과
- Windows x64 Agent 및 오프라인 패키지 빌드·감사 통과

## 적용 조건

Web v4.7.37과 Agent v1.3.24를 함께 사용합니다. Agent 설치 프로그램은 실행 중인 이전 Agent를 종료하고 기존 프로그램 파일을 제거한 뒤 새 Agent와 Web 파일을 설치합니다. 사용자 data, logs, output 및 VPDL Worker 선택값은 유지됩니다.
