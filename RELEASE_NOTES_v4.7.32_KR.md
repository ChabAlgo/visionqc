# VisionQC v4.7.32 / Agent v1.3.19

## 변경 내용

- 저장된 CSV/XLSX 결과 파일을 다시 불러오면 이전에 선택했던 날짜 필터를 자동으로 해제합니다.
- 결과 파일을 교체하거나 제거하거나 Input 전체를 초기화한 뒤 메인 대시보드를 현재 입력 전체로 다시 집계합니다.
- Local Agent 실행부는 변경하지 않았으며 v1.3.19를 그대로 사용합니다.

## 원인

메인 대시보드에서 날짜를 선택한 상태로 다른 결과 파일을 불러오면 파일 파싱과 분석 모델 교체는 정상적으로 끝났지만, 이전 날짜 선택값이 남았습니다. 새 로그에 그 날짜가 없으면 모든 새 행이 대시보드 필터에서 제외되어 갱신되지 않은 것처럼 보였습니다.

## 검증 범위

- 서로 다른 두 날짜의 기존 결과에서 날짜 하나를 선택한 뒤, 날짜가 다른 저장 CSV를 설정 화면에서 다시 불러왔습니다.
- 불러오기 완료 후 날짜 선택이 해제되고 새 Cell·Position 한 건이 메인 대시보드에 표시되는지 브라우저에서 확인했습니다.
- CSV와 XLSX는 파싱 후 같은 결과 교체 경로를 사용하므로 두 형식 모두 수정이 적용됩니다.
