# VisionQC 로그인·다중 PC 동기화 검토 (v4.7.9)

## 결론

Google 계정으로 로그인하고, 사용자별 설정/검사 이력을 다른 PC에서도 이어 사용하려면 인터넷에서 접근 가능한 인증·데이터 저장 서비스가 필요합니다. 현재 GitHub Pages와 각 PC의 Local Agent만으로는 사용자 계정 또는 안전한 동기화를 제공할 수 없습니다.

## 권장 역할 분리

| 영역 | 권장 위치 | 동기화 여부 |
| --- | --- | --- |
| Google 로그인, 사용자 ID | 관리형 Auth 서비스 | 동기화 |
| Position, Tool Threshold, 파일명 규칙, 화면 설정 | 사용자별 클라우드 DB | 동기화 |
| Simulation/CSV 결과의 집계값과 이력 메타데이터 | 사용자별 클라우드 DB | 동기화 |
| Cognex VPDL, GPU, Windows Picker, 실제 로컬 경로 | Local Agent | 동기화하지 않음 |
| 원본 Grab 이미지와 대용량 CSV | 로컬 디스크 | 기본 동기화하지 않음 |

## 권장 구현안

1. Supabase Auth의 Google OAuth로 로그인하고, DB의 모든 사용자 데이터에 `user_id`와 Row Level Security를 적용합니다.
2. Web은 로그인 토큰으로 설정·이력 요약만 동기화합니다. Windows 절대 경로는 다른 PC에서 무효하고 민감할 수 있으므로 로컬 전용으로 유지합니다.
3. Local Agent는 계속 `127.0.0.1`에서 VPDL/파일을 처리합니다. 이미지나 원본 CSV 업로드는 기본값으로 금지하고, 향후 명시적 선택·용량·보존기간 정책을 별도 설계합니다.
4. 오프라인 실행은 Agent SQLite와 브라우저 로컬 캐시를 우선 사용하고, 네트워크 복구 및 로그인 상태에서 변경 큐를 동기화합니다.

## 단계 제안

- 1단계: Google 로그인, 사용자별 설정 동기화, 로컬 경로는 제외
- 2단계: Simulation/CSV 이력의 중복 제거된 메타데이터 동기화
- 3단계: 공유 리포트와 권한 관리
- 4단계: 필요성이 승인된 경우에만 이미지/원본 파일의 별도 저장소 업로드

## 사전 결정이 필요한 사항

- 계정이 개인별인지, 팀/공정별 공동 사용인지
- 이력과 원본 이미지의 보존 기간 및 삭제 권한
- 사내망만 사용할지, 외부 인터넷에서도 접근할지
- 오프라인 중 생성한 이력의 충돌 처리 기준

이번 v4.7.9에는 서버, OAuth 키, 외부 데이터 전송을 추가하지 않습니다.
