# VisionQC v4.7.38 / Agent v1.3.25

## 원인

날짜별 그래프는 각 날짜 안에서 `Cell ID + Position`을 중복 제거했지만 전체보기는 날짜를 제외한 `Cell ID + Position`만 사용했습니다. 같은 Cell ID가 여러 날짜에 다시 등장하면 서로 다른 검사 결과와 Tool 판정이 하나의 검사로 합쳐졌습니다.

이 때문에 날짜별 건수의 합계보다 전체보기 건수가 작아지고, 어느 날짜에서든 NG였던 Tool이 전체 통합 결과를 NG로 만들면서 전체 NG율이 실제보다 높아질 수 있었습니다.

## 수정 내용

- 분석 집계 키를 `날짜 + Cell ID + Position`으로 통일했습니다.
- 같은 날짜의 중복 행은 기존과 같이 한 검사로 합치며, 날짜가 다르면 별도 검사로 계산합니다.
- Cell별 NG도 `날짜 + Cell ID` 기준으로 계산해 날짜별 Cell 합계와 전체보기가 일치합니다.
- 실행 중 증분 집계와 완료 후 전체 재계산이 같은 키를 사용합니다.
- 실제 NG 이미지도 파일명 촬영 날짜를 포함해 결과와 연결합니다.
- 검사 이력 DB의 화면 중복 제거에 촬영 날짜를 추가했습니다. 동일 날짜·Cell·Position·Workspace의 반복 기록은 최신 기록을 유지합니다.
- 미검 CSV와 전체 결과 CSV에 `CaptureTimestamp`를 포함하고, 전체 결과에는 원본·처리 경로와 원본 CSV 위치도 함께 기록합니다.

## 실제 데이터 대조

2026-09-15 최신 로컬 실행 DB를 읽기 전용으로 확인했습니다.

- Agent 원본 저장 행: 488건
- 기존 전체보기: 124 Cell·Position / 122 NG / 98.39%
- 날짜별 기준 합계: 370 Cell·Position / 298 NG / 80.54%
- Cell 기준 합계: 185 Cell / 149 NG / 80.54%
- 2026-02-03 선택 결과: 54 Cell·Position / 38 NG / 70.37%

수정 후 전체보기는 일곱 날짜의 날짜별 집계 합계와 같은 370건·298 NG를 사용합니다.

## 검증 기준

- 서로 다른 날짜의 동일 Cell ID가 별도 record로 유지되는 브라우저 회귀
- 같은 날짜의 동일 Cell ID·Position은 계속 중복 제거되는 정적 회귀
- 전체보기의 검사·NG 합계가 날짜별 검사·NG 합계와 일치하는 회귀
- 실행 중 증분 집계와 완료 전체 재계산 동등성 회귀
- 200,000행 증분 성능 회귀
- 검사 이력 날짜 중복 제거 SQL과 날짜 인덱스 정적 회귀

## 적용 조건

Web v4.7.38과 Agent v1.3.25를 함께 사용합니다. 설치 프로그램은 이전 Agent 프로그램 파일을 교체하며 사용자 `data`, `logs`, `output`, Workspace와 원본 이미지는 삭제하지 않습니다.
