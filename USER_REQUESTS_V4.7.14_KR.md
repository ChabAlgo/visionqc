# VisionQC v4.7.14 사용자 요청 및 반영 기준

## 요청 사항

1. 메인 화면의 Tool Threshold 숫자 입력에서 위·아래 조절 화살표가 잘리지 않아야 한다.
2. 분석 화면의 분석 범위 설명을 Position·Tool·분석 범위 선택 상자 바로 아래에 배치한다.
3. 시뮬레이션 진행 표시가 갱신될 때 사용자의 클릭이 무시되거나 드롭다운이 닫히지 않아야 한다.
4. Cell별 Score 그래프 점을 눌러 여는 이미지 뷰어는 100%에서 이미지 전체를 표시한다.
5. 동일한 검사 이미지가 CSV FullPath와 실제 NG 이미지 양쪽에서 발견돼도 뷰어에는 한 번만 표시한다.
6. 병합된 실제 NG 이미지는 기존의 이미지 제외 기능을 유지한다.
7. 이미지 뷰어의 방향키와 좌우 버튼은 Cell별 Score 그래프에 표시된 낮은 Score 순서대로 이전·다음 이미지를 연다.
8. 원본·Crop·Heatmap 사이를 전환하거나 다음 Score 이미지로 이동해도 확대 배율과 이동 위치를 유지한다.

## 구현 기준

- 실시간 시뮬레이션 결과는 화면을 매 건 즉시 교체하지 않고 짧게 묶어서 반영한다.
- 포인터 입력 중에는 화면 전체 재렌더링을 미루고 클릭이 끝난 뒤 적용한다.
- Tool 집계 과정에서도 저장된 Heatmap Overlay 경로를 유지한다.
- 이미지 중복 판정은 동일 Cell ID·Position 안에서 동일 경로 또는 동일 파일명을 기준으로 하며, 원본과 Crop은 서로 다른 보기로 유지한다.
- Score 이미지 탐색 순서는 Score, Position, Cell ID, 원본 행 순서로 결정해 그래프와 키보드 탐색을 동일하게 유지한다.

## 검증 항목

- Threshold 입력 크기와 기본 숫자 스피너 표시
- 분석 범위 설명의 DOM 배치 순서
- 실시간 갱신 중 드롭다운 클릭 유지
- 동일 이미지 1개 표시와 실제 NG 제외 버튼 유지
- 100% 전체 이미지 맞춤
- Heatmap 선택 및 확대 상태에서 Score 순서 방향키 이동
