# VisionQC v4.7.36 / Agent v1.3.23

## 수정 내용

실행 중 새 결과가 들어올 때마다 브라우저가 이전 누적 결과 전체를 다시 집계하던 방식을 증분 집계로 변경했습니다. 새 배치에 포함된 Cell, Position, Tool의 카운트와 Score 통계만 갱신합니다.

동일 Cell ID와 Position이 다시 들어오면 기존 레코드의 집계 기여분을 먼저 제거하고 원본 행을 합친 뒤 다시 반영합니다. Threshold 판정, 실제 NG 매칭, 미검, Tool별 NG와 최소 Score도 전체 재계산과 같은 규칙을 사용합니다.

## 누락·중복 안전 장치

- 실행 ID와 마지막 SQLite `image_id`를 커서로 사용해 다음 새 행만 조회합니다.
- 한 번에 여러 페이지를 읽는 도중 실패하면 화면과 커서를 갱신하지 않아 다음 시도에서 같은 범위를 다시 읽습니다.
- DB 실시간 동기화 중 SSE 상세행을 별도로 추가하지 않습니다.
- 완료 시 Agent 처리 건수, SQLite 저장 건수, 조회 건수와 화면 변환 건수가 모두 같아야 결과를 교체합니다.
- 완료 전체 재동기화는 증분 상태를 버리고 DB 원본으로 새로 구성합니다.

## 성능 확인

- 실제 Cognex Runtime 2,412건, 4 Position: 193.79초, 12.31건/초
- 실시간 결과 조회 82회: 평균 94ms, 최대 276ms, 누락 0건
- 브라우저 합성 200,000건, 25건씩 8,000회 증분 반영: 전체 집계 약 1.4초

## 검증

- 증분 집계와 전체 재계산의 Cell·Position·Tool·실제 NG·중복 결과 동등성 브라우저 회귀 통과
- 실행 중 DB 증분 수신 및 완료 전체 재동기화 회귀 통과
- 정적 회귀 테스트 139개 통과
- 전체 브라우저 회귀 테스트 47개 통과
- Windows x64 Release 빌드 오류 0개
- 실제 Cognex VPDL 4.0 Runtime: 4 Position 12건 처리, 결과 12건·Tool 결과 48건·Heatmap 16건 확인
- 설치된 Agent 1.3.23 및 내장 Web 4.7.36 확인
- 배포 패키지 리소스 및 SHA-256 감사 통과

## 적용 조건

Web v4.7.36과 Agent v1.3.23을 함께 사용합니다. 새 Agent 번호로 설치해 기존 버전을 제거하고 v4.7.36 Web 자산까지 교체합니다.
