# VisionQC v4.7.31 / Agent v1.3.19

## 변경 내용

- Cell별 Score 그래프에서 연 이미지가 그래프와 동일하게 낮은 Score부터 높은 Score 순서로 이동합니다.
- 첫 이미지에서 왼쪽, 마지막 이미지에서 오른쪽 방향키를 눌러도 더 진행하지 않습니다.
- 날짜별 검사 NG율 그래프의 첫 점과 마지막 점을 좌우 축 끝에서 안쪽으로 이동해 여백을 확보했습니다. 날짜 선택 영역은 그래프 전체 폭을 유지합니다.
- Position별 Tool NG 구성의 `실제 NG 최소`와 `Threshold`를 각각 줄바꿈해 Tool 수가 많아도 겹치거나 잘리지 않게 했습니다.
- Runtime File Load가 선택 Stream에서 확인한 최상위 Green/Red Tool을 Tool Settings에 자동으로 추가합니다. 기존 Tool, Threshold, 판정 설정은 유지합니다.
- 기본 목록에 없던 `Edge` 같은 Tool도 Agent 검사 요청과 결과 CSV에 포함되므로 검출이 0건이어도 분석 화면의 Tool 목록에 남습니다.

## 원인

- Score 점은 그래프 내부에서만 정렬되고 이미지 뷰어 목록은 CSV 행 순서를 사용하고 있었습니다.
- 날짜 그래프의 X 좌표가 첫 점 0%, 마지막 점 100% 위치를 그대로 사용했습니다.
- 대시보드 전용 CSS가 두 설명을 한 줄로 강제하면서 좁은 Tool 카드에서 잘렸습니다.
- Runtime 구조 화면은 Workspace의 모든 Tool을 표시했지만 실제 검사는 별도의 고정 기본 Tool 설정만 Agent에 전달했습니다. Separator는 기본 목록에 있어 표시됐고 Edge는 기본 목록에 없어 결과 열 자체가 생성되지 않았습니다.

## 검증 범위

- 정렬되지 않은 CSV 행을 사용해 0.61 → 0.72 → 0.83 순서와 마지막 경계 정지를 브라우저에서 확인했습니다.
- 날짜 그래프 양끝 점이 축 경계 안쪽에 있고 날짜별 클릭 영역이 유지되는지 확인했습니다.
- Runtime에서 발견한 Edge가 한 번만 설정에 추가되고 Agent 요청에도 포함되는지 확인했습니다.
- FHD 화면에서 Tool 카드의 설명·Threshold가 겹치지 않고 입력칸이 카드 안에 유지되는지 확인했습니다.
- Agent 실행 엔진은 변경하지 않았으며 기존 v1.3.19를 사용합니다.
