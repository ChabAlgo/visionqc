# 로그인 / 프로젝트별 DB 검토

현재 구조 확인: 2026-09-17. 이번 릴리스에는 아래 기능을 구현하지 않습니다.

## 현재 구조

- 웹 화면은 GitHub Pages 또는 오프라인 정적 파일로 실행합니다. Local Agent가 SQLite 조회·저장·삭제를 처리합니다.
- 기본 DB는 `%LOCALAPPDATA%\VisionQC\LocalAgent\data\visionqc-history.sqlite` 하나입니다. `AgentServer.ResolveHistoryDatabasePath()`에 테스트·복구용 경로 재정의가 있지만 프로젝트 전환 기능은 아닙니다.
- Workspace 검색 조건이 있지만 프로젝트별 데이터/권한 격리를 의미하지 않습니다.

## 권장 1단계: 오프라인 프로젝트 관리

로그인 없이도 프로젝트 선택과 DB 분리는 가능합니다. 현재 오프라인 사용 방식에는 이 단계가 먼저 적합합니다.

1. 프로젝트 ID·표시명·생성일을 관리하는 목록과 생성/선택 화면을 추가합니다.
2. 표시명 대신 검증한 내부 ID로 `projects/<id>/history.sqlite`를 분리합니다. 프로젝트별 백업/복원이 쉬워집니다.
3. 시뮬레이션 시작 시 프로젝트 ID와 저장 대상 DB를 고정하고, 실행·CSV 가져오기·삭제 중 전환을 막거나 작업에 고정된 DB를 계속 사용하도록 설계합니다. 작업 중 선택 변경으로 다른 DB에 결과가 들어가면 안 됩니다.
4. 이력 검색·CSV 저장·삭제·이미지 조회·진행률 요청에도 프로젝트 ID를 전달합니다. 오래된 비동기 응답은 현재 프로젝트 화면에 적용하지 않습니다.
5. Threshold, 파일명 규칙, 입력 경로, 대시보드 캐시도 프로젝트별로 구분합니다. 공통 설정과 프로젝트 설정을 명확하게 나눕니다.
6. 기존 DB는 기본 프로젝트로 등록하고, 백업·건수 대조 후 전환합니다. 원본을 임의로 삭제하지 않습니다.
7. 전환 중 실행, 동시 탭, 재시작, 실패한 가져오기, 프로젝트별 삭제·복원, 대량 데이터 테스트가 필요합니다.

DB만 나누는 작업보다 화면 상태·진행 중 작업·CSV 경로까지 같은 프로젝트에 묶는 작업이 중요합니다. 이 단계는 데이터 정리 기능이며 사용자 접근 통제 기능은 아닙니다.

## 2단계: 로그인과 권한 관리

- 한 PC에서 사용자 기록만 남길 목적이라면 로컬 사용자 관리와 작업 기록이 가능합니다. 다만 파일에 접근 가능한 Windows 사용자를 웹 로그인만으로 차단할 수는 없습니다.
- 여러 PC에서 같은 프로젝트를 공유하고 접근 권한을 제한하려면 사내 서버, 인증/세션 관리, 사용자-프로젝트 권한, 서버 측 API 권한 검사, 감사 기록, 백업 체계가 필요합니다. 정적 웹 화면에 비밀번호 입력창만 추가해서는 보호되지 않습니다.
- 공유 SQLite 파일을 여러 PC에서 직접 열기보다는 서버 API가 DB를 관리하도록 설계해야 합니다. 각 PC의 이미지 경로도 서로 다를 수 있어 공유 경로 규칙이나 이미지 제공 방식이 필요합니다.
- 완전 오프라인 PC와 중앙 로그인/공유를 동시에 요구하면 오프라인 인증 정책과 결과 동기화·충돌 처리 범위가 추가됩니다.

구현 전에 결정할 항목은 (1) PC별 독립 관리인지 여러 PC 공동 사용인지, (2) 프로젝트마다 분리할 설정 범위, (3) 사용자별 조회/실행/삭제 권한 필요 여부입니다. 현재 사용 형태에는 프로젝트별 로컬 DB부터 도입하고, 공동 사용이 필요해질 때 로그인·서버를 추가하는 순서를 권장합니다.
