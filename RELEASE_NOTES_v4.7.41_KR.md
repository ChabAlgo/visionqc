# VisionQC 4.7.41 / Local Agent 1.3.28

## 변경 사항
- 시뮬레이션 종료·중지·오류에서 Elapsed와 Speed가 고정되며 ETA는 0으로 표시됩니다. Runtime File Load를 새로 실행하면 시간·속도를 초기화합니다.
- 시뮬레이션 완료 후 화면 아래에 DB 저장 Yes/No를 표시합니다. Yes를 선택해야 영구 검사 이력 DB에 들어갑니다. No여도 CSV와 현재 대시보드는 유지됩니다.
- 실시간 증분 집계와 연결 끊김 복구에 필요한 원본 결과는 별도 임시 DB에 기록합니다. 영구 검사 이력과 분리되며 다음 시뮬레이션 시작 시 임시 내용이 교체됩니다. 따라서 실행 중 임시 기록은 계속 사용되지만 영구 이력은 자동 증가하지 않습니다.
- 저장은 실행 ID 기준 한 트랜잭션으로 처리합니다. 같은 저장 요청을 재시도해도 중복되지 않으며, 저장 선택을 마치기 전 다음 실행을 시작하지 않습니다. 병렬 Position 내부 작업은 별도 질문 없이 전체 작업 완료 후 한 번만 선택합니다.
- 설정의 SQLite 분석 이력은 같은 영구 DB에 현재 CSV를 수동 저장하는 기능입니다. 기존 수동 CSV 저장 및 대용량 CSV 직접 저장은 유지됩니다.
- 검사 이력의 Position → 검사 모드 → Workspace → Tool 선택을 연동합니다. 상위 조건을 바꾸면 하위 선택을 초기화하고 해당 조건에 실제 존재하는 항목만 조회합니다.
- 촬영/검사 시각은 `2026-09-16 / 08:34:26`처럼 표시합니다. 원본 시각·소수초·시간대와 DB 집계 기준은 그대로 보존하며 표시 숫자의 시간대를 임의로 바꾸지 않습니다.
- 시작일/종료일 입력칸 전체 클릭으로 달력을 엽니다(브라우저 showPicker 지원 시).
- 분류 화면 상단 Cell ID Excel 버튼: XLSX 파일 하나에 라벨별 시트로 Cell ID만 저장합니다. 다중 라벨은 각각의 시트에, 같은 라벨의 중복 ID는 한 번만 기록합니다. 미분류 이미지는 제외하며 현재 파일명 규칙으로 ID 추출에 실패한 라벨 이미지가 있으면 누락 방지를 위해 내보내기를 중단하고 알립니다. 시트명 금지 문자·31자 길이·중복 이름을 처리합니다.
- 원본 이미지 복사·이동과 PNG 변환은 이 변경에 포함하지 않습니다.

## 검증 결과
- 정적 회귀 검사 144개 통과, Chromium 회귀 검사 60개 통과(20만 행 증분 집계 정합성 검사 포함).
- 별도 임시 SQLite에서 401행 승인 저장, 반복 승인 시 중복 방지, 미승인 시 영구 DB 보존, Position별 Tool/Workspace 목록, Score와 원본 시각 보존 검증.
- 실제 VPDL 4.0/API 8.0, RTX 4050 Laptop, 제공된 Score_Test_Add 워크스페이스와 Test_Image 61장으로 새 Agent 검증: 단일 Position 2회, 2개 Position 병렬 2회(총 366건). 완료 후 시간 고정, Runtime 재로드 시 초기화, No 시 미저장, Yes 재시도 시 1회 저장, 완료 결과의 전체 행 복구 및 병렬 연속 실행 통과.
- API 8.0/8.2/Universal/Core 및 x64 설치 프로그램 빌드. 실제 이번 검사 실행은 Green/API 8.0이며 Blue·Integrated 모델 실검사는 이번 범위에 포함하지 않았습니다.
- 테스트는 별도 포트 18941 및 임시 DB를 사용했습니다. 실행 중인 기존 Agent, 사용자 이미지·Workspace·기존 DB를 교체하거나 수정하지 않았습니다.

## 적용
웹 새로고침과 Agent 1.3.28 설치가 필요합니다. 구버전 Agent에서 자동 영구 저장이 계속되는 일을 막기 위해 새 웹의 시뮬레이션 시작은 해당 Agent 버전을 요구합니다. PNG 처리 방식과 Score 계산 엔진은 변경하지 않았습니다.
