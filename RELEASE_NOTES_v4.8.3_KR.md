# VisionQC 4.8.3 / Agent 1.4.3

- 일반 CSV 내보내기와 최종 통합 결과에서는 `.csv.parts.txt`를 생성하지 않습니다.
- 포지션 병렬 시뮬레이션은 모든 분할 파일을 빠짐없이 통합할 때까지 내부 목록을 유지하고, 통합이 성공하면 해당 목록만 자동 삭제합니다. 단일 실행도 정상 완료 후 목록을 삭제합니다.
- 통합이 실패한 경우에는 재처리에 필요한 입력 목록을 보존합니다. CSV, 이미지, Workspace, DB는 이 정리 기능의 삭제 대상이 아닙니다.
- 날짜만 있고 Time이 빈 집계 CSV를 Agent에 다시 불러올 때 날짜를 보존합니다. 이전에는 날짜가 미인식되어 서로 다른 날짜의 같은 Cell이 합쳐질 수 있었습니다. 알 수 없는 시간을 임의로 채우지 않습니다.
- 날짜만 있는 결과를 다시 CSV로 내보낼 때도 Date를 유지합니다.
- 검증 경로: 두 CSV 다중 입력 → 날짜/Position/Threshold 집계 → 80건을 날짜별 8개 CSV로 저장 → 텍스트 목록 없이 8개 재입력 → 80건 유지 및 선택 Tool NG 10건 재저장.
- 분할 목록 정리 전후 38개 CSV 레코드 유지, 줄바꿈 포함 CSV, 날짜별/행 수 분할, 병합 실패 시 목록 보존, 단일 Position 정리 검증을 추가했습니다.

설치 후 화면은 4.8.3, 연결된 Agent는 1.4.3인지 확인하세요. 이미 날짜를 잘못 인식해 불러온 분석은 새 Agent에서 원본 CSV들을 다시 선택하여 불러와야 합니다. 기존 저장 DB를 자동 변경하지 않습니다.
