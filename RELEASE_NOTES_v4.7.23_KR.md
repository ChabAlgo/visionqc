# VisionQC 4.7.23 / Agent 1.3.13

## 결론과 범위

H100 보안 서버의 원인은 아직 확정되지 않았다. 이번 버전은 무작정 GPU 설정을 바꾸는 대신
**원본 엔진과 현재 엔진 / 기본 DLL 검색과 선택 SDK 검색 고정**의 차이를 같은 입력으로 구분한다.
실제 원본 엔진 두 소스를 바이트 그대로 포함한 진단 실행기를 추가했으며 정상 검사 기능은 유지한다.

현장 증거에서 Sample.Process가 먼저 6cc4a157 예외를 기록한 뒤 같은 PID가 ucrtbase.dll /
0xC0000409로 종료됐다. 이것만으로 최초 실패가 정리 코드 때문이거나 ucrtbase.dll 파일이
손상됐다고 판단할 수 없다. 두 GPU를 각각 선택해도 실패했고, 최근 독립 실행에서는 생성·검사
스레드가 동일했다. 같은 Workspace/이미지는 기존 프로그램에서 정상이라는 사용자의 확인이 있다.

## 추가·수정

- 바탕화면 **VisionQC Green 비교 진단**: 최근 독립 Green 검사 이미지 1장을 로컬 복사해 네 조합을 순차 실행.
- 원본 ZIP의 GreenOverlayProcessor.cs / Models.cs는 변경하지 않는다. 출처 해시는 GreenBaseline/README_KR.md에 기록.
- 원본 GUI 전체와 동일한 EXE라는 뜻은 아니다. 진단 진입점과 빌드는 새로 만들었다.
- 각 실행의 PID, 종료 코드, 최초 SDK 예외, 실제 로드 DLL 경로·버전·SHA-256과 요청 기록을 보존.
- 각 SDK 작업의 성공/실패 기록과 별도로 Sample.Dispose / Image.Dispose / Control.Dispose의 시작·완료·예외를 기록.
- 최초 SDK 실패 이후 비정상 종료가 생겨도 웹 오류에 원래 SDK 오류와 마지막 정리 단계를 같이 전달.
- 실행별 logs/green-runs 폴더에도 기록하여 공유 last-*.txt 갱신에 의해 이전 실행 증거가 사라지지 않도록 보완.

정리 단계의 BEGIN 이후 종료가 보이면 해당 단계가 미완료였다는 뜻이다.
다른 네이티브 스레드의 비동기 종료 가능성까지 배제하여 그 단계가 원인이라고 확정하는 것은 아니다.

## 서버에서 할 일

1. 새 ZIP의 EXE를 설치한다. 웹 **4.7.23**, Agent **1.3.13**을 확인한다.
2. Green의 **원본 방식 · 독립 Green 검사** 및 **상세 진단 로그**를 켜고
   기존 Workspace·이미지·GPU 번호 그대로 Runtime File Load → Simulation Start를 한 번 실행한다.
3. 종료/실패 후 바탕화면 **VisionQC Green 비교 진단** → **비교 시작** → 확인을 누른다.
4. 자동으로 열리는 결과요약.txt에서 **A/B/C/D 네 줄과 오류 문구**를 확인한다.

각 조합은 최대 3분이다. 중지 버튼은 이번 진단이 만든 자식만 종료한다.
비교 진단 중 일반 VisionQC 검사를 동시에 시작하지 않는다.
이미지·Workspace·DB·전체 DLL 목록은 반출할 필요가 없다. 보안 정책에 따라 서버 안에 보관한다.

| 비교 결과 | 다음 확인 방향 — 확정 원인은 아님 |
| --- | --- |
| A/C 성공, B/D 실패 | 선택 SDK DLL 검색 고정이 로드 모듈을 바꾸는지 확인 |
| A/B 성공, C/D 실패 | 현재 엔진·진단·호스트 차이를 좁혀 확인 |
| 네 경우 모두 실패, 실제 원본 GUI 정상 | 원본 GUI의 실제 DLL·환경과 비교 필요 |
| 네 경우 모두 성공 | 단일 이미지에서 미재현. 전체 실행 조건·시점 차이 확인 |

다른 결과 조합은 SDK 실패 단계와 모듈 기록을 함께 검토한다.
성공 여부만 비교하며 판정/Score 일치가 자동 보장되는 것은 아니다.

## 유지한 기능 / 변경하지 않은 것

일반 검사의 원본 독립 실행, Integrated·Blue·기존 Green 옵션, 사전 로드, Tool/Threshold/판정,
ROI·히트맵·CSV·SQLite·실시간 진행률, 날짜시간 규칙, 분류·뷰어·테마를 유지한다.
GPU 번호·드라이버·TCC/WDDM·학습 프로세스·시스템 환경변수는 변경하지 않는다.
기본 검사에서 DLL 검색 고정은 계속 유지한다. 기본 검색 모드는 별도 비교 진단에만 허용한다.

진단에만 한 장·한 Position으로 제한하며 원본 엔진의 고정 Position 지원을 위해 내부 키를
CA_TOP으로 매핑한다. 두 엔진에 같은 요청을 사용한다. 선택이 끝난 이미지이므로 진단에서만
Cell ID/Keyword 필터를 우회하며 기존 설정 파일을 저장하거나 덮어쓰지 않는다.
진단 결과는 SQLite에 넣지 않는다.

## 검증

- 코드 회귀시험 123개, 브라우저 29개 통과.
- C# 파일명 16 / Runtime 정책 11 / SDK 진단 19 / 자식 프로세스 경계 26 / 비교 실행기 12 = 84개 통과.
- 이 PC의 VPDL 4.0 및 4.2에서 각각 이미지 6개, Tool 결과 24개, 히트맵 8개와 SQLite 저장 확인.
- 기존 Green 실행(독립 실행 해제)과 Universal Worker의 VPDL 4.0 실행도 각각 6/24/8 및 SQLite 저장 통과.
- 같은 이미지 한 장을 사용한 실제 VPDL 4.0 A/B/C/D 네 조합 모두 성공. 조합당 약 23초.
- 원본 ZIP 엔진과 새 독립 실행·Agent 연동의 48개 Score 비교: 최대 절대 차이 0.000071,
  시험 허용치 0.0001 이내, 모든 비교 대상 판정 일치. 비트 단위 동일성을 보장하는 것은 아니다.
- 실제 손상 이미지에서 Image.Load 실패가 원래 SDK 예외와 함께 보존되고 결과 0건임을 확인.
- 가짜 자식이 SDK 오류를 남긴 뒤 0xC0000409로 종료하는 시험에서 최초 오류·정리 단계가 함께 전달됨을 확인.
- VPDL 미설치 Core의 오프라인 웹·이력 기능 실행 및 시뮬레이션 거절 동작 확인.
- 설치 EXE 내부에 API 8.0 / 8.2 / Universal 비교 실행기가 포함됨을 검사. Cognex DLL·검사 데이터 미포함.

실제 H100/Server 2022 보안 서버의 현장 재현은 여기서 수행할 수 없다.
이 PC에서 성공했다는 결과가 보안 서버 오류 해결을 뜻하지 않는다.
현재 성공 시험에서도 기본 DLL 검색과 고정 검색의 실제 모듈 경로 일부는 달랐다.
이 차이만으로 현장 오류 원인이라고 판단하지 않는다.

## 빌드 / 기록

BUILD_VPDL_WORKERS.ps1 → OfflineInstaller 프로젝트 Release x64 빌드 →
버전별 downloads EXE 복사 → BUILD_OFFLINE_PACKAGE.ps1 순서로 패키징한다.
패키징 스크립트는 EXE·사용 안내·릴리스 노트·VERSION.txt만 허용한다.
설치 파일과 ZIP의 SHA-256은 RELEASE_MANIFEST.json에 기록한다.
이전 배포 EXE/ZIP은 복구 및 비교용으로 보존한다.
